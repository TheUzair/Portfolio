export const metadata = {
  title: "Projects | <NextMode />",
  description: "Explore my latest projects and case studies as a full-stack developer.",
};

export default function ProjectsLayout({ children }) {
  return <>{children}</>;
}