export const metadata = {
  title: "Reserve a Spot | <NextMode />",
  description: "Reserve a spot for a project or consultation.",
};

export default function ProjectsLayout({ children }) {
  return <>{children}</>;
}