export const metadata = {
  title: "Articles | <NextMode />",
  description: "Read my latest articles on web development, design, and more.",
};

export default function ProjectsLayout({ children }) {
  return <>{children}</>;
}